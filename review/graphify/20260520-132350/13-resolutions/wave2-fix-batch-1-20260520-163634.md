# Wave 2 fix batch result

## Approved batch

- Selector: 1
- Batch name: Wave 2 Batch 1 - Harden standalone Argo CD resume checkpoint reads

## Issues fixed

### Standalone Argo CD resume reads checkpoint through weaker safe-path validation

- Severity: Important
- Source files:
  - `ansible_collections/tomazb/acm_switchover/playbooks/argocd_resume.yml`
  - `ansible_collections/tomazb/acm_switchover/plugins/modules/acm_safe_path_validate.py`
  - `ansible_collections/tomazb/acm_switchover/tests/unit/test_argocd_hub_parameterization.py`
- Root cause: `argocd_resume.yml` validated the persisted checkpoint path before `stat`/`slurp`, but did not set `path_type: artifact`, so `acm_safe_path_validate` used the default weaker `safe` policy instead of the symlink-aware artifact policy used for controller-side report/checkpoint artifacts.
- Fix: Set `path_type: artifact` on the standalone Argo CD resume checkpoint validation task before any controller-side checkpoint `stat` or `slurp`. Updated the module documentation/example to describe artifact validation as covering controller-side artifact reads and writes.
- Tests added or updated: Updated `test_standalone_argocd_resume_validates_checkpoint_path_before_file_reads` to parse `argocd_resume.yml`, verify validation still precedes `stat`, and assert that the validation task uses `path_type: artifact`.
- Verification commands:
  - `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local pytest ansible_collections/tomazb/acm_switchover/tests/unit/test_argocd_hub_parameterization.py -q`
  - `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local pytest ansible_collections/tomazb/acm_switchover/tests/unit/test_ansible_resilience_contracts.py -q`
  - `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local pytest ansible_collections/tomazb/acm_switchover/tests/unit/plugins/module_utils/test_validation_security.py -q`
  - `./run_tests.sh`
- Result: Fixed. The new contract test failed before the playbook change because `path_type` was absent, then passed after the fix. The full runner reached quality gates and failed on repository-wide pre-existing style/quality findings; targeted verification passed.

## Issues not fixed

None.

## Verification summary

- Commands run:
  - `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local pytest ansible_collections/tomazb/acm_switchover/tests/unit/test_argocd_hub_parameterization.py -q` - 19 passed
  - `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local pytest ansible_collections/tomazb/acm_switchover/tests/unit/test_ansible_resilience_contracts.py -q` - 35 passed
  - `ANSIBLE_LOCAL_TEMP=/tmp/ansible-local pytest ansible_collections/tomazb/acm_switchover/tests/unit/plugins/module_utils/test_validation_security.py -q` - 35 passed
  - `./run_tests.sh` - exited 1 at quality gates after the unit test phase reached completion; failures were existing flake8/pylint/Black findings across unrelated files. The Black gate includes `test_argocd_hub_parameterization.py`, but the `HEAD` version of that file is already Black-noncompliant (`.venv/bin/black --check /tmp/orig_argocd_hub_parameterization.py` also fails).
- Commands skipped: Live-cluster and destructive operations.
- Why skipped: The mission explicitly says not to run destructive real-cluster operations.
- Failures: Expected red-test failure before implementation: `test_standalone_argocd_resume_validates_checkpoint_path_before_file_reads` reported missing `path_type: artifact`. Broad `./run_tests.sh` failed on pre-existing quality gates unrelated to the Batch 1 functional fix.
- Final status: Targeted verification passed; full runner is blocked by existing repository quality-gate debt.

## Git diff summary

- `ansible_collections/tomazb/acm_switchover/playbooks/argocd_resume.yml`: standalone checkpoint validation now uses `path_type: artifact`.
- `ansible_collections/tomazb/acm_switchover/plugins/modules/acm_safe_path_validate.py`: documentation/example now describe artifact validation for controller-side artifact reads and writes.
- `ansible_collections/tomazb/acm_switchover/tests/unit/test_argocd_hub_parameterization.py`: checkpoint validation contract now asserts artifact policy usage before file reads.

## Next recommended batch

Wave 2 Batch 2 - Align preflight report check mode with artifact writer.
