---
all:
  hosts:
    localhost:
      ansible_connection: local
      # Use the same interpreter as ansible-playbook so controller-side modules
      # (for example kubernetes.core.k8s_info nested from action plugins) resolve
      # packages installed alongside ansible-core.
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
