---
# ClusterRole for ACM Switchover Decommission
# Grants the cluster-scoped reads and deletes needed for hub teardown.
# Baseline operator RBAC already grants MCO get/list/delete for normal finalization,
# but a standalone decommission run does not carry the baseline operator ClusterRole.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: acm-switchover-decommission
  labels:
    app.kubernetes.io/name: acm-switchover
    app.kubernetes.io/component: decommission
    app.kubernetes.io/role: operator
rules:
  - apiGroups: ["hive.openshift.io"]
    resources: ["clusterdeployments"]
    verbs: ["list"]
  - apiGroups: ["cluster.open-cluster-management.io"]
    resources: ["managedclusters"]
    verbs: ["delete"]
  - apiGroups: ["operator.open-cluster-management.io"]
    resources: ["multiclusterhubs"]
    verbs: ["delete"]
  # The standalone teardown in modules/decommission.py reads the named
  # MultiClusterObservability before deleting it and again for the final absence proof,
  # so this extension must grant `get` for the extension to be self-sufficient.
  - apiGroups: ["observability.open-cluster-management.io"]
    resources: ["multiclusterobservabilities"]
    verbs: ["get", "delete"]
