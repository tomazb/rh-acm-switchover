---
- name: Emit finalization foundation contract
  ansible.builtin.set_fact:
    acm_switchover_phase_result:
      phase: finalization
      status: foundation_only
      changed: false
  tags:
    - finalization
