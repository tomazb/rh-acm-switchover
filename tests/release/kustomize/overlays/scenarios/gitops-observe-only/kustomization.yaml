apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../../bases/argocd-acm-safe-mode/observe-only
- scenario-sentinel.yaml

labels:
- includeSelectors: true
  pairs:
    acm-switchover.redhat-lab/gitops-mode: observe-only
