# V2: Invalid kubeconfig path
acm_switchover_hubs:
  primary:
    context: mgmt2
    kubeconfig: /nonexistent/path/kubeconfig
  secondary:
    context: mgmt1
    kubeconfig: /nonexistent/path/kubeconfig
acm_switchover_operation:
  method: passive
acm_switchover_execution:
  mode: validate
  report_dir: ./artifacts
