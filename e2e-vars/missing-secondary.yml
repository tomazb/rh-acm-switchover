# V1: Missing secondary context
acm_switchover_hubs:
  primary:
    context: mgmt2
    kubeconfig: ~/.kube/config
  # secondary intentionally omitted
acm_switchover_operation:
  method: passive
acm_switchover_execution:
  mode: validate
  report_dir: ./artifacts
